@PACKAGE_INIT@

get_filename_component(se_interface_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(CMakeFindDependencyMacro)

if(NOT TARGET se_interace)
    include("${se_interface_CMAKE_DIR}/se_interface-targets.cmake")
endif()

check_required_components(se_interface)
