@PACKAGE_INIT@

get_filename_component(protocol_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(CMakeFindDependencyMacro)
find_dependency(ifx-error REQUIRED)
find_dependency(logger REQUIRED)

if(NOT TARGET ifx::protocol)
    include("${protocol_CMAKE_DIR}/protocol-targets.cmake")
endif()

check_required_components(protocol)
